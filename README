latex/ contains the latex sources
java/ods contains the java sources

To make the book (ods.pdf):
cd latex ; make
This will require a decent installation of pdflatex, perl, ipe, and pdftk.

To make the Java archive ods.jar:
cd java ; make

To make everything:
make

