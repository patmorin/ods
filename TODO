

This release:
- Fix snarf.pl so that it picks up static variables that are initialized
- SortedSMap wrapper

USet interface
- USet -> Set wrapper
- USet -> Map wrapper


Sometime:
- Make a version of snarf.pl that produces output suitable for latex2html
- SkiplistRope implementation
- Talk about iteration and finger search in skiplists


Next release:
- Chapter on data structures for integers (bounded universes)
